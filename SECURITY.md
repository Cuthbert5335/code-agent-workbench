# Security Policy

## 支持范围

当前只支持主分支和最新公开版本。项目仍是本地优先的学习与开发工具，不承诺在线生产环境的安全等级。

## 报告安全问题

请不要在公开 Issue 中粘贴 API Key、密码、会话令牌、数据库文件、上传代码或完整任务轨迹。请通过 GitHub Security Advisories（如果仓库已启用）或维护者指定的私密联系方式报告问题，并尽可能提供：

- 受影响的版本或提交。
- 可复现步骤和最小化示例。
- 影响范围和建议修复方向。

收到报告后，维护者会先确认问题、评估影响，再决定是否发布修复和安全公告。

## 已知安全边界

- `backend/.env` 只用于本地服务端配置，不能提交到仓库。
- 补丁只更新后端 SQLite 快照，不直接修改用户本地文件。
- 沙箱只允许固定命令并要求显式确认，不可用时不会降级到宿主机执行。
- 不要把 CodeXXX 暴露到公网，除非你已经自行完成身份验证、HTTPS、网络隔离和数据保护加固。
